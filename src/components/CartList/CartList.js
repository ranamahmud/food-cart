import React, { Fragment } from 'react';
import CartItems from '../CartItems/CartItems';

const CartList = () => {
    return (
        <Fragment>
            <CartItems />
            <CartItems />
        </Fragment>

    );
};

export default CartList;